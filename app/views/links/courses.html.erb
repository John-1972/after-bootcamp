<h1>This is the advanced courses page.</h1> <!-- Text to identify this page -->

<h4>Overview of Advanced Course Options</h4>
<% @options_links.each do |link| %>
  <a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a>
  <br />
<% end %>

<h4>Cryptocurrency</h4>
<% @crypto_links.each do |link| %>
  <a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a>
  <br />
<% end %>

<h4>Cyber Security</h4>
<% @cybersec_links.each do |link| %>
  <a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a>
  <br />
<% end %>