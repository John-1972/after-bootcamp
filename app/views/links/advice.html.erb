<div class="container">
  <div class="titlebox col-md"> <!-- Title and sub-title -->
    <h1>Job-Search Advice</h1>
    <h3>This is an evolving list of useful resources to assist bootcamp graduates with their job hunt.</h3>
  </div>
</div>

<div class="container">
  <div class="section-heading col-md">
    Skills In Demand <!-- Section heading -->
  </div>
  <ul>
    <div class="bullet-point col-md"> <!-- Links as bullet points -->
      <% @skill_links.each do |link| %>
        <li><b><a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a></b></li>
      <% end %>
    </div>
  </ul>
</div>

<div class="container">
  <div class="section-heading col-md">
    Web Developer Pay Rates <!-- Section heading -->
  </div>
  <ul>
    <div class="bullet-point col-md"> <!-- Links as bullet points -->
      <% @wage_links.each do |link| %>
        <li><b><a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a></b></li>
      <% end %>
    </div>
  </ul>
</div>

<div class="container">
  <div class="section-heading col-md">
    Advice on Finding a Job <!-- Section heading -->
  </div>
  <ul>
    <div class="bullet-point col-md"> <!-- Links as bullet points -->
      <% @jobsearch_links.each do |link| %>
        <li><b><a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a></b></li>
      <% end %>
    </div>
  </ul>
</div>

<div class="container">
  <div class="section-heading col-md">
    Improving Your Resumé <!-- Section heading -->
  </div>
  <ul>
    <div class="bullet-point col-md"> <!-- Links as bullet points -->
      <% @resume_links.each do |link| %>
        <li><b><a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a></b></li>
      <% end %>
    </div>
  </ul>
</div>

<div class="container">
  <div class="section-heading col-md">
    Building Your Portfolio <!-- Section heading -->
  </div>
  <ul>
    <div class="bullet-point col-md"> <!-- Links as bullet points -->
      <% @portfolio_links.each do |link| %>
        <li><b><a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a></b></li>
      <% end %>
    </div>
  </ul>
</div>

<div class="container">
  <div class="section-heading col-md">
    Preparing for Interviews <!-- Section heading -->
  </div>
  <ul>
    <div class="bullet-point col-md"> <!-- Links as bullet points -->
      <% @interview_links.each do |link| %>
        <li><b><a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a></b></li>
      <% end %>
    </div>
  </ul>
</div>

<div class="container">
  <div class="section-heading col-md">
    Data Structures, Algorithms & Big-O Notation <!-- Section heading -->
  </div>
  <ul>
    <div class="bullet-point col-md"> <!-- Links as bullet points -->
      <% @data_algo_links.each do |link| %>
        <li><b><a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a></b></li>
      <% end %>
    </div>
  </ul>
</div>

<div class="container">
  <div class="section-heading col-md">
    Example Interview Questions <!-- Section heading -->
  </div>
  <ul>
    <div class="bullet-point col-md"> <!-- Links as bullet points -->
      <% @question_links.each do |link| %>
        <li><b><a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a></b></li>
      <% end %>
    </div>
  </ul>
</div>

<div class="container">
  <div class="section-heading col-md">
    Helpful YouTube Channels <!-- Section heading -->
  </div>
  <ul>
    <div class="bullet-point col-md"> <!-- Links as bullet points -->
      <% @youtube_links.each do |link| %>
        <li><b><a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a></b></li>
      <% end %>
    </div>
  </ul>
</div>