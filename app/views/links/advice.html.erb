<h1>This is the job advice page.</h1> <!-- Text to identify this page -->

<h4>Skills In Demand</h4>
<% @skill_links.each do |link| %>
  <a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a>
  <br />
<% end %>

<h4>Web Dev Pay Rates</h4>
<% @wage_links.each do |link| %>
  <a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a>
  <br />
<% end %>

<h4>Advice on Finding a Job</h4>
<% @jobsearch_links.each do |link| %>
  <a target="_blank" rel="noopener noreferrer" href='<%= link.URL %>'><%= link.title %></a>
  <br />
<% end %>